import { eventFeature, gpsFeature, ioFeature, keywords } from 'notify-services';

const SERVER_URL = 'http://localhost:3003';

const main = async () => {
    try {
    } catch (error) {
        console.error('Error sending notification:', error);
    }
};

main();
